import type {CapacitorConfig} from '@capacitor/cli';
const config:CapacitorConfig={appId:'app.broquer.mobile',appName:'Broquer',webDir:'mobile-dist',ios:{contentInset:'never'},server:{iosScheme:'capacitor'}};
export default config;
